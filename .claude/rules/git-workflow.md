# Git Workflow

- Work on `feature/<short-description>` branches
- Never commit or push directly to `main` / `master`
- Commits in English, imperative: `feat:`, `fix:`, `docs:`, `refactor:`
- One logical change per commit when possible
- Run `/verify` before PR
- Content changes: note in PR that therapist should review Dutch copy
